# --- Web framework (Phase 0/1) ---
fastapi==0.115.0
pydantic==2.12.5
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- LLM (Phase 1) ---
google-genai==2.20.0

# --- LangChain (Phase 2) ---
langchain==1.1.1
langchain-google-genai==4.4.0

# --- RAG (Phase 3) ---
langchain-text-splitters==1.0.0
langchain-chroma==1.0.0
chromadb==1.0.20
# --- LangGraph (Phase 7) ---
langgraph==1.0.2
# --- Tests (Phase 11) ---
pytest==8.3.3
httpx==0.28.1
# --- Qualité de code (Phase 12) ---
ruff==0.7.4
# --- Base de données ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.9

# --- Data (ingestion + futur tool Pandas) ---
pandas==2.2.3



# Les dépendances LangChain, LangGraph, SQLAlchemy, ChromaDB, Pandas
# seront ajoutées progressivement à chaque phase (voir méthode de travail).
# On évite d'installer d'un coup tout ce dont on n'a pas encore besoin.